import styled from "styled-components";

const GraphicStyled = styled.section`
  display: flex;
  flex-direction: column;

  .title {
    font-size: 1rem;
    align-self: flex-start;
  }
`;

export default GraphicStyled;
